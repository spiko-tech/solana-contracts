/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findMinterConfigPda } from "../pdas";
import { MINTER_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const NOMINATE_ADMIN_DISCRIMINATOR = new Uint8Array([
  134, 11, 31, 244, 20, 77, 138, 121,
]);

export function getNominateAdminDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    NOMINATE_ADMIN_DISCRIMINATOR,
  );
}

export type NominateAdminInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMinterConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMinterConfig extends string
        ? WritableAccount<TAccountMinterConfig>
        : TAccountMinterConfig,
      ...TRemainingAccounts,
    ]
  >;

export type NominateAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAdmin: Address;
};

export type NominateAdminInstructionDataArgs = { newAdmin: Address };

export function getNominateAdminInstructionDataEncoder(): FixedSizeEncoder<NominateAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAdmin", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: NOMINATE_ADMIN_DISCRIMINATOR }),
  );
}

export function getNominateAdminInstructionDataDecoder(): FixedSizeDecoder<NominateAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAdmin", getAddressDecoder()],
  ]);
}

export function getNominateAdminInstructionDataCodec(): FixedSizeCodec<
  NominateAdminInstructionDataArgs,
  NominateAdminInstructionData
> {
  return combineCodec(
    getNominateAdminInstructionDataEncoder(),
    getNominateAdminInstructionDataDecoder(),
  );
}

export type NominateAdminAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMinterConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  minterConfig?: Address<TAccountMinterConfig>;
  newAdmin: NominateAdminInstructionDataArgs["newAdmin"];
};

export async function getNominateAdminInstructionAsync<
  TAccountAdmin extends string,
  TAccountMinterConfig extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: NominateAdminAsyncInput<TAccountAdmin, TAccountMinterConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  NominateAdminInstruction<TProgramAddress, TAccountAdmin, TAccountMinterConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    minterConfig: { value: input.minterConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.minterConfig.value) {
    accounts.minterConfig.value = await findMinterConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.minterConfig),
    ],
    data: getNominateAdminInstructionDataEncoder().encode(
      args as NominateAdminInstructionDataArgs,
    ),
    programAddress,
  } as NominateAdminInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig
  >);
}

export type NominateAdminInput<
  TAccountAdmin extends string = string,
  TAccountMinterConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  minterConfig: Address<TAccountMinterConfig>;
  newAdmin: NominateAdminInstructionDataArgs["newAdmin"];
};

export function getNominateAdminInstruction<
  TAccountAdmin extends string,
  TAccountMinterConfig extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: NominateAdminInput<TAccountAdmin, TAccountMinterConfig>,
  config?: { programAddress?: TProgramAddress },
): NominateAdminInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMinterConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    minterConfig: { value: input.minterConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.minterConfig),
    ],
    data: getNominateAdminInstructionDataEncoder().encode(
      args as NominateAdminInstructionDataArgs,
    ),
    programAddress,
  } as NominateAdminInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig
  >);
}

export type ParsedNominateAdminInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    minterConfig: TAccountMetas[1];
  };
  data: NominateAdminInstructionData;
};

export function parseNominateAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedNominateAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), minterConfig: getNextAccount() },
    data: getNominateAdminInstructionDataDecoder().decode(instruction.data),
  };
}
