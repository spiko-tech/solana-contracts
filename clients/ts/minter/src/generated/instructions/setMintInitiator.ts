/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findMinterConfigPda } from "../pdas";
import { MINTER_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_MINT_INITIATOR_DISCRIMINATOR = new Uint8Array([
  23, 155, 142, 181, 54, 109, 37, 106,
]);

export function getSetMintInitiatorDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MINT_INITIATOR_DISCRIMINATOR,
  );
}

export type SetMintInitiatorInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMinterConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMinterConfig extends string
        ? WritableAccount<TAccountMinterConfig>
        : TAccountMinterConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetMintInitiatorInstructionData = {
  discriminator: ReadonlyUint8Array;
  newInitiator: Address;
};

export type SetMintInitiatorInstructionDataArgs = { newInitiator: Address };

export function getSetMintInitiatorInstructionDataEncoder(): FixedSizeEncoder<SetMintInitiatorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newInitiator", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MINT_INITIATOR_DISCRIMINATOR }),
  );
}

export function getSetMintInitiatorInstructionDataDecoder(): FixedSizeDecoder<SetMintInitiatorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newInitiator", getAddressDecoder()],
  ]);
}

export function getSetMintInitiatorInstructionDataCodec(): FixedSizeCodec<
  SetMintInitiatorInstructionDataArgs,
  SetMintInitiatorInstructionData
> {
  return combineCodec(
    getSetMintInitiatorInstructionDataEncoder(),
    getSetMintInitiatorInstructionDataDecoder(),
  );
}

export type SetMintInitiatorAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMinterConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  minterConfig?: Address<TAccountMinterConfig>;
  newInitiator: SetMintInitiatorInstructionDataArgs["newInitiator"];
};

export async function getSetMintInitiatorInstructionAsync<
  TAccountAdmin extends string,
  TAccountMinterConfig extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: SetMintInitiatorAsyncInput<TAccountAdmin, TAccountMinterConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMintInitiatorInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    minterConfig: { value: input.minterConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.minterConfig.value) {
    accounts.minterConfig.value = await findMinterConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.minterConfig),
    ],
    data: getSetMintInitiatorInstructionDataEncoder().encode(
      args as SetMintInitiatorInstructionDataArgs,
    ),
    programAddress,
  } as SetMintInitiatorInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig
  >);
}

export type SetMintInitiatorInput<
  TAccountAdmin extends string = string,
  TAccountMinterConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  minterConfig: Address<TAccountMinterConfig>;
  newInitiator: SetMintInitiatorInstructionDataArgs["newInitiator"];
};

export function getSetMintInitiatorInstruction<
  TAccountAdmin extends string,
  TAccountMinterConfig extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: SetMintInitiatorInput<TAccountAdmin, TAccountMinterConfig>,
  config?: { programAddress?: TProgramAddress },
): SetMintInitiatorInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMinterConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    minterConfig: { value: input.minterConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.minterConfig),
    ],
    data: getSetMintInitiatorInstructionDataEncoder().encode(
      args as SetMintInitiatorInstructionDataArgs,
    ),
    programAddress,
  } as SetMintInitiatorInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig
  >);
}

export type ParsedSetMintInitiatorInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    minterConfig: TAccountMetas[1];
  };
  data: SetMintInitiatorInstructionData;
};

export function parseSetMintInitiatorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMintInitiatorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), minterConfig: getNextAccount() },
    data: getSetMintInitiatorInstructionDataDecoder().decode(instruction.data),
  };
}
