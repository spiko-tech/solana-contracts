/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findMinterConfigPda } from "../pdas";
import { MINTER_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_MINTER_DISCRIMINATOR = 0;

export function getInitializeMinterDiscriminatorBytes() {
  return getU8Encoder().encode(INITIALIZE_MINTER_DISCRIMINATOR);
}

export type InitializeMinterInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> =
    "9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh",
  TAccountSelfProgram extends string | AccountMeta<string> =
    "3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountSelfProgram extends string
        ? ReadonlyAccount<TAccountSelfProgram>
        : TAccountSelfProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeMinterInstructionData = {
  discriminator: number;
  maxDelay: bigint;
  permissionManager: Address;
};

export type InitializeMinterInstructionDataArgs = {
  maxDelay: number | bigint;
  permissionManager: Address;
};

export function getInitializeMinterInstructionDataEncoder(): FixedSizeEncoder<InitializeMinterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["maxDelay", getI64Encoder()],
      ["permissionManager", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_MINTER_DISCRIMINATOR }),
  );
}

export function getInitializeMinterInstructionDataDecoder(): FixedSizeDecoder<InitializeMinterInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["maxDelay", getI64Decoder()],
    ["permissionManager", getAddressDecoder()],
  ]);
}

export function getInitializeMinterInstructionDataCodec(): FixedSizeCodec<
  InitializeMinterInstructionDataArgs,
  InitializeMinterInstructionData
> {
  return combineCodec(
    getInitializeMinterInstructionDataEncoder(),
    getInitializeMinterInstructionDataDecoder(),
  );
}

export type InitializeMinterAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountSelfProgram extends string = string,
> = {
  /** Admin / deployer (payer for PDA creation) */
  admin: TransactionSigner<TAccountAdmin>;
  /** MinterConfig PDA to be created */
  config?: Address<TAccountConfig>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA for CPI event emission */
  eventAuthority?: Address<TAccountEventAuthority>;
  /** Minter program (self) for CPI event emission */
  selfProgram?: Address<TAccountSelfProgram>;
  maxDelay: InitializeMinterInstructionDataArgs["maxDelay"];
  permissionManager: InitializeMinterInstructionDataArgs["permissionManager"];
};

export async function getInitializeMinterInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountSelfProgram extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: InitializeMinterAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeMinterInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    selfProgram: { value: input.selfProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findMinterConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value =
      "9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh" as Address<"9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh">;
  }
  if (!accounts.selfProgram.value) {
    accounts.selfProgram.value =
      "3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2" as Address<"3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.selfProgram),
    ],
    data: getInitializeMinterInstructionDataEncoder().encode(
      args as InitializeMinterInstructionDataArgs,
    ),
    programAddress,
  } as InitializeMinterInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >);
}

export type InitializeMinterInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountSelfProgram extends string = string,
> = {
  /** Admin / deployer (payer for PDA creation) */
  admin: TransactionSigner<TAccountAdmin>;
  /** MinterConfig PDA to be created */
  config: Address<TAccountConfig>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA for CPI event emission */
  eventAuthority?: Address<TAccountEventAuthority>;
  /** Minter program (self) for CPI event emission */
  selfProgram?: Address<TAccountSelfProgram>;
  maxDelay: InitializeMinterInstructionDataArgs["maxDelay"];
  permissionManager: InitializeMinterInstructionDataArgs["permissionManager"];
};

export function getInitializeMinterInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountSelfProgram extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: InitializeMinterInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeMinterInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountSelfProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    selfProgram: { value: input.selfProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value =
      "9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh" as Address<"9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh">;
  }
  if (!accounts.selfProgram.value) {
    accounts.selfProgram.value =
      "3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2" as Address<"3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.selfProgram),
    ],
    data: getInitializeMinterInstructionDataEncoder().encode(
      args as InitializeMinterInstructionDataArgs,
    ),
    programAddress,
  } as InitializeMinterInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >);
}

export type ParsedInitializeMinterInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin / deployer (payer for PDA creation) */
    admin: TAccountMetas[0];
    /** MinterConfig PDA to be created */
    config: TAccountMetas[1];
    /** System program */
    systemProgram: TAccountMetas[2];
    /** Event authority PDA for CPI event emission */
    eventAuthority: TAccountMetas[3];
    /** Minter program (self) for CPI event emission */
    selfProgram: TAccountMetas[4];
  };
  data: InitializeMinterInstructionData;
};

export function parseInitializeMinterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeMinterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      selfProgram: getNextAccount(),
    },
    data: getInitializeMinterInstructionDataDecoder().decode(instruction.data),
  };
}
