/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { MINTER_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_MAX_DELAY_DISCRIMINATOR = 5;

export function getSetMaxDelayDiscriminatorBytes() {
  return getU8Encoder().encode(SET_MAX_DELAY_DISCRIMINATOR);
}

export type SetMaxDelayInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPermConfig extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> =
    "9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh",
  TAccountSelfProgram extends string | AccountMeta<string> =
    "3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPermConfig extends string
        ? ReadonlyAccount<TAccountPermConfig>
        : TAccountPermConfig,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountSelfProgram extends string
        ? ReadonlyAccount<TAccountSelfProgram>
        : TAccountSelfProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetMaxDelayInstructionData = {
  discriminator: number;
  maxDelay: bigint;
};

export type SetMaxDelayInstructionDataArgs = { maxDelay: number | bigint };

export function getSetMaxDelayInstructionDataEncoder(): FixedSizeEncoder<SetMaxDelayInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["maxDelay", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MAX_DELAY_DISCRIMINATOR }),
  );
}

export function getSetMaxDelayInstructionDataDecoder(): FixedSizeDecoder<SetMaxDelayInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["maxDelay", getI64Decoder()],
  ]);
}

export function getSetMaxDelayInstructionDataCodec(): FixedSizeCodec<
  SetMaxDelayInstructionDataArgs,
  SetMaxDelayInstructionData
> {
  return combineCodec(
    getSetMaxDelayInstructionDataEncoder(),
    getSetMaxDelayInstructionDataDecoder(),
  );
}

export type SetMaxDelayInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountPermConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountSelfProgram extends string = string,
> = {
  /** Admin caller */
  caller: TransactionSigner<TAccountCaller>;
  /** MinterConfig PDA */
  config: Address<TAccountConfig>;
  /** PermissionConfig PDA (from permission_manager, for admin check) */
  permConfig: Address<TAccountPermConfig>;
  /** Event authority PDA for CPI event emission */
  eventAuthority?: Address<TAccountEventAuthority>;
  /** Minter program (self) for CPI event emission */
  selfProgram?: Address<TAccountSelfProgram>;
  maxDelay: SetMaxDelayInstructionDataArgs["maxDelay"];
};

export function getSetMaxDelayInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountPermConfig extends string,
  TAccountEventAuthority extends string,
  TAccountSelfProgram extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: SetMaxDelayInput<
    TAccountCaller,
    TAccountConfig,
    TAccountPermConfig,
    TAccountEventAuthority,
    TAccountSelfProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetMaxDelayInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountPermConfig,
  TAccountEventAuthority,
  TAccountSelfProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    permConfig: { value: input.permConfig ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    selfProgram: { value: input.selfProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value =
      "9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh" as Address<"9iz29VQ2wSuy56NKTz9qBv9mNpFP4bApHtbc2aPzURhh">;
  }
  if (!accounts.selfProgram.value) {
    accounts.selfProgram.value =
      "3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2" as Address<"3pXknoeMQiY44nKBcnwtSSxzuh1uxUHPHggjXcuVLDT2">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.permConfig),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.selfProgram),
    ],
    data: getSetMaxDelayInstructionDataEncoder().encode(
      args as SetMaxDelayInstructionDataArgs,
    ),
    programAddress,
  } as SetMaxDelayInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountPermConfig,
    TAccountEventAuthority,
    TAccountSelfProgram
  >);
}

export type ParsedSetMaxDelayInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin caller */
    caller: TAccountMetas[0];
    /** MinterConfig PDA */
    config: TAccountMetas[1];
    /** PermissionConfig PDA (from permission_manager, for admin check) */
    permConfig: TAccountMetas[2];
    /** Event authority PDA for CPI event emission */
    eventAuthority: TAccountMetas[3];
    /** Minter program (self) for CPI event emission */
    selfProgram: TAccountMetas[4];
  };
  data: SetMaxDelayInstructionData;
};

export function parseSetMaxDelayInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMaxDelayInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      permConfig: getNextAccount(),
      eventAuthority: getNextAccount(),
      selfProgram: getNextAccount(),
    },
    data: getSetMaxDelayInstructionDataDecoder().decode(instruction.data),
  };
}
