/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findMinterConfigPda, findPendingTransferPda } from "../pdas";
import { MINTER_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const NOMINATE_MINT_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  36, 192, 79, 215, 27, 29, 30, 24,
]);

export function getNominateMintAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    NOMINATE_MINT_AUTHORITY_DISCRIMINATOR,
  );
}

export type NominateMintAuthorityInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMinterConfig extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountPendingTransfer extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMinterConfig extends string
        ? ReadonlyAccount<TAccountMinterConfig>
        : TAccountMinterConfig,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountPendingTransfer extends string
        ? WritableAccount<TAccountPendingTransfer>
        : TAccountPendingTransfer,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type NominateMintAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type NominateMintAuthorityInstructionDataArgs = {
  newAuthority: Address;
};

export function getNominateMintAuthorityInstructionDataEncoder(): FixedSizeEncoder<NominateMintAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: NOMINATE_MINT_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getNominateMintAuthorityInstructionDataDecoder(): FixedSizeDecoder<NominateMintAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getNominateMintAuthorityInstructionDataCodec(): FixedSizeCodec<
  NominateMintAuthorityInstructionDataArgs,
  NominateMintAuthorityInstructionData
> {
  return combineCodec(
    getNominateMintAuthorityInstructionDataEncoder(),
    getNominateMintAuthorityInstructionDataDecoder(),
  );
}

export type NominateMintAuthorityAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMinterConfig extends string = string,
  TAccountMint extends string = string,
  TAccountPendingTransfer extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  minterConfig?: Address<TAccountMinterConfig>;
  mint: Address<TAccountMint>;
  pendingTransfer?: Address<TAccountPendingTransfer>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: NominateMintAuthorityInstructionDataArgs["newAuthority"];
};

export async function getNominateMintAuthorityInstructionAsync<
  TAccountAdmin extends string,
  TAccountMinterConfig extends string,
  TAccountMint extends string,
  TAccountPendingTransfer extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: NominateMintAuthorityAsyncInput<
    TAccountAdmin,
    TAccountMinterConfig,
    TAccountMint,
    TAccountPendingTransfer,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  NominateMintAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig,
    TAccountMint,
    TAccountPendingTransfer,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    minterConfig: { value: input.minterConfig ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    pendingTransfer: { value: input.pendingTransfer ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.minterConfig.value) {
    accounts.minterConfig.value = await findMinterConfigPda();
  }
  if (!accounts.pendingTransfer.value) {
    accounts.pendingTransfer.value = await findPendingTransferPda({
      mint: expectAddress(accounts.mint.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.minterConfig),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.pendingTransfer),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getNominateMintAuthorityInstructionDataEncoder().encode(
      args as NominateMintAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as NominateMintAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig,
    TAccountMint,
    TAccountPendingTransfer,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type NominateMintAuthorityInput<
  TAccountAdmin extends string = string,
  TAccountMinterConfig extends string = string,
  TAccountMint extends string = string,
  TAccountPendingTransfer extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  minterConfig: Address<TAccountMinterConfig>;
  mint: Address<TAccountMint>;
  pendingTransfer: Address<TAccountPendingTransfer>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  newAuthority: NominateMintAuthorityInstructionDataArgs["newAuthority"];
};

export function getNominateMintAuthorityInstruction<
  TAccountAdmin extends string,
  TAccountMinterConfig extends string,
  TAccountMint extends string,
  TAccountPendingTransfer extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MINTER_PROGRAM_ADDRESS,
>(
  input: NominateMintAuthorityInput<
    TAccountAdmin,
    TAccountMinterConfig,
    TAccountMint,
    TAccountPendingTransfer,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): NominateMintAuthorityInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMinterConfig,
  TAccountMint,
  TAccountPendingTransfer,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MINTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    minterConfig: { value: input.minterConfig ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    pendingTransfer: { value: input.pendingTransfer ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.minterConfig),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.pendingTransfer),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getNominateMintAuthorityInstructionDataEncoder().encode(
      args as NominateMintAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as NominateMintAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMinterConfig,
    TAccountMint,
    TAccountPendingTransfer,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedNominateMintAuthorityInstruction<
  TProgram extends string = typeof MINTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    minterConfig: TAccountMetas[1];
    mint: TAccountMetas[2];
    pendingTransfer: TAccountMetas[3];
    payer: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: NominateMintAuthorityInstructionData;
};

export function parseNominateMintAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedNominateMintAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      minterConfig: getNextAccount(),
      mint: getNextAccount(),
      pendingTransfer: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getNominateMintAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
