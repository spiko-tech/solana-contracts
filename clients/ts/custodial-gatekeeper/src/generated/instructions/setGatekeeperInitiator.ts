/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findGatekeeperConfigPda } from "../pdas";
import { CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_GATEKEEPER_INITIATOR_DISCRIMINATOR = new Uint8Array([
  149, 219, 23, 160, 73, 14, 56, 98,
]);

export function getSetGatekeeperInitiatorDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_GATEKEEPER_INITIATOR_DISCRIMINATOR,
  );
}

export type SetGatekeeperInitiatorInstruction<
  TProgram extends string = typeof CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountGatekeeperConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountGatekeeperConfig extends string
        ? WritableAccount<TAccountGatekeeperConfig>
        : TAccountGatekeeperConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetGatekeeperInitiatorInstructionData = {
  discriminator: ReadonlyUint8Array;
  newInitiator: Address;
};

export type SetGatekeeperInitiatorInstructionDataArgs = {
  newInitiator: Address;
};

export function getSetGatekeeperInitiatorInstructionDataEncoder(): FixedSizeEncoder<SetGatekeeperInitiatorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newInitiator", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_GATEKEEPER_INITIATOR_DISCRIMINATOR,
    }),
  );
}

export function getSetGatekeeperInitiatorInstructionDataDecoder(): FixedSizeDecoder<SetGatekeeperInitiatorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newInitiator", getAddressDecoder()],
  ]);
}

export function getSetGatekeeperInitiatorInstructionDataCodec(): FixedSizeCodec<
  SetGatekeeperInitiatorInstructionDataArgs,
  SetGatekeeperInitiatorInstructionData
> {
  return combineCodec(
    getSetGatekeeperInitiatorInstructionDataEncoder(),
    getSetGatekeeperInitiatorInstructionDataDecoder(),
  );
}

export type SetGatekeeperInitiatorAsyncInput<
  TAccountAdmin extends string = string,
  TAccountGatekeeperConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gatekeeperConfig?: Address<TAccountGatekeeperConfig>;
  newInitiator: SetGatekeeperInitiatorInstructionDataArgs["newInitiator"];
};

export async function getSetGatekeeperInitiatorInstructionAsync<
  TAccountAdmin extends string,
  TAccountGatekeeperConfig extends string,
  TProgramAddress extends Address = typeof CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS,
>(
  input: SetGatekeeperInitiatorAsyncInput<
    TAccountAdmin,
    TAccountGatekeeperConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetGatekeeperInitiatorInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGatekeeperConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    gatekeeperConfig: {
      value: input.gatekeeperConfig ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.gatekeeperConfig.value) {
    accounts.gatekeeperConfig.value = await findGatekeeperConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gatekeeperConfig),
    ],
    data: getSetGatekeeperInitiatorInstructionDataEncoder().encode(
      args as SetGatekeeperInitiatorInstructionDataArgs,
    ),
    programAddress,
  } as SetGatekeeperInitiatorInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGatekeeperConfig
  >);
}

export type SetGatekeeperInitiatorInput<
  TAccountAdmin extends string = string,
  TAccountGatekeeperConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gatekeeperConfig: Address<TAccountGatekeeperConfig>;
  newInitiator: SetGatekeeperInitiatorInstructionDataArgs["newInitiator"];
};

export function getSetGatekeeperInitiatorInstruction<
  TAccountAdmin extends string,
  TAccountGatekeeperConfig extends string,
  TProgramAddress extends Address = typeof CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS,
>(
  input: SetGatekeeperInitiatorInput<TAccountAdmin, TAccountGatekeeperConfig>,
  config?: { programAddress?: TProgramAddress },
): SetGatekeeperInitiatorInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountGatekeeperConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    gatekeeperConfig: {
      value: input.gatekeeperConfig ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gatekeeperConfig),
    ],
    data: getSetGatekeeperInitiatorInstructionDataEncoder().encode(
      args as SetGatekeeperInitiatorInstructionDataArgs,
    ),
    programAddress,
  } as SetGatekeeperInitiatorInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGatekeeperConfig
  >);
}

export type ParsedSetGatekeeperInitiatorInstruction<
  TProgram extends string = typeof CUSTODIAL_GATEKEEPER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    gatekeeperConfig: TAccountMetas[1];
  };
  data: SetGatekeeperInitiatorInstructionData;
};

export function parseSetGatekeeperInitiatorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetGatekeeperInitiatorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), gatekeeperConfig: getNextAccount() },
    data: getSetGatekeeperInitiatorInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
