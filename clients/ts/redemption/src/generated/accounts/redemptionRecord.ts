/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const REDEMPTION_RECORD_DISCRIMINATOR = new Uint8Array([
  225, 49, 176, 110, 91, 172, 28, 16,
]);

export function getRedemptionRecordDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REDEMPTION_RECORD_DISCRIMINATOR,
  );
}

export type RedemptionRecord = {
  discriminator: ReadonlyUint8Array;
  bump: number;
};

export type RedemptionRecordArgs = { bump: number };

/** Gets the encoder for {@link RedemptionRecordArgs} account data. */
export function getRedemptionRecordEncoder(): FixedSizeEncoder<RedemptionRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REDEMPTION_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RedemptionRecord} account data. */
export function getRedemptionRecordDecoder(): FixedSizeDecoder<RedemptionRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RedemptionRecord} account data. */
export function getRedemptionRecordCodec(): FixedSizeCodec<
  RedemptionRecordArgs,
  RedemptionRecord
> {
  return combineCodec(
    getRedemptionRecordEncoder(),
    getRedemptionRecordDecoder(),
  );
}

export function decodeRedemptionRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RedemptionRecord, TAddress>;
export function decodeRedemptionRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RedemptionRecord, TAddress>;
export function decodeRedemptionRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<RedemptionRecord, TAddress>
  | MaybeAccount<RedemptionRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRedemptionRecordDecoder(),
  );
}

export async function fetchRedemptionRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RedemptionRecord, TAddress>> {
  const maybeAccount = await fetchMaybeRedemptionRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRedemptionRecord<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RedemptionRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRedemptionRecord(maybeAccount);
}

export async function fetchAllRedemptionRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RedemptionRecord>[]> {
  const maybeAccounts = await fetchAllMaybeRedemptionRecord(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRedemptionRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RedemptionRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeRedemptionRecord(maybeAccount),
  );
}

export function getRedemptionRecordSize(): number {
  return 9;
}
