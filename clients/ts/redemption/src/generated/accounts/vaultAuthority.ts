/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VAULT_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  132, 34, 187, 202, 202, 195, 211, 53,
]);

export function getVaultAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VAULT_AUTHORITY_DISCRIMINATOR,
  );
}

export type VaultAuthority = {
  discriminator: ReadonlyUint8Array;
  bump: number;
};

export type VaultAuthorityArgs = { bump: number };

/** Gets the encoder for {@link VaultAuthorityArgs} account data. */
export function getVaultAuthorityEncoder(): FixedSizeEncoder<VaultAuthorityArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VAULT_AUTHORITY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VaultAuthority} account data. */
export function getVaultAuthorityDecoder(): FixedSizeDecoder<VaultAuthority> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VaultAuthority} account data. */
export function getVaultAuthorityCodec(): FixedSizeCodec<
  VaultAuthorityArgs,
  VaultAuthority
> {
  return combineCodec(getVaultAuthorityEncoder(), getVaultAuthorityDecoder());
}

export function decodeVaultAuthority<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VaultAuthority, TAddress>;
export function decodeVaultAuthority<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VaultAuthority, TAddress>;
export function decodeVaultAuthority<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VaultAuthority, TAddress> | MaybeAccount<VaultAuthority, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVaultAuthorityDecoder(),
  );
}

export async function fetchVaultAuthority<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VaultAuthority, TAddress>> {
  const maybeAccount = await fetchMaybeVaultAuthority(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVaultAuthority<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VaultAuthority, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVaultAuthority(maybeAccount);
}

export async function fetchAllVaultAuthority(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VaultAuthority>[]> {
  const maybeAccounts = await fetchAllMaybeVaultAuthority(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVaultAuthority(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VaultAuthority>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeVaultAuthority(maybeAccount),
  );
}

export function getVaultAuthoritySize(): number {
  return 9;
}
