/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findRedemptionConfigPda } from "../pdas";
import { REDEMPTION_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_REDEMPTION_DISCRIMINATOR = 0;

export function getInitializeRedemptionDiscriminatorBytes() {
  return getU8Encoder().encode(INITIALIZE_REDEMPTION_DISCRIMINATOR);
}

export type InitializeRedemptionInstruction<
  TProgram extends string = typeof REDEMPTION_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountSelfProgram extends string | AccountMeta<string> =
    "8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountSelfProgram extends string
        ? ReadonlyAccount<TAccountSelfProgram>
        : TAccountSelfProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeRedemptionInstructionData = {
  discriminator: number;
  permissionManager: Address;
};

export type InitializeRedemptionInstructionDataArgs = {
  permissionManager: Address;
};

export function getInitializeRedemptionInstructionDataEncoder(): FixedSizeEncoder<InitializeRedemptionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["permissionManager", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_REDEMPTION_DISCRIMINATOR,
    }),
  );
}

export function getInitializeRedemptionInstructionDataDecoder(): FixedSizeDecoder<InitializeRedemptionInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["permissionManager", getAddressDecoder()],
  ]);
}

export function getInitializeRedemptionInstructionDataCodec(): FixedSizeCodec<
  InitializeRedemptionInstructionDataArgs,
  InitializeRedemptionInstructionData
> {
  return combineCodec(
    getInitializeRedemptionInstructionDataEncoder(),
    getInitializeRedemptionInstructionDataDecoder(),
  );
}

export type InitializeRedemptionAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountSelfProgram extends string = string,
> = {
  /** Admin / deployer (payer for PDA creation) */
  admin: TransactionSigner<TAccountAdmin>;
  /** RedemptionConfig PDA (to be created) */
  config?: Address<TAccountConfig>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Redemption program (self) for CPI event emission */
  selfProgram?: Address<TAccountSelfProgram>;
  permissionManager: InitializeRedemptionInstructionDataArgs["permissionManager"];
};

export async function getInitializeRedemptionInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountSelfProgram extends string,
  TProgramAddress extends Address = typeof REDEMPTION_PROGRAM_ADDRESS,
>(
  input: InitializeRedemptionAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeRedemptionInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REDEMPTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    selfProgram: { value: input.selfProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findRedemptionConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.selfProgram.value) {
    accounts.selfProgram.value =
      "8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX" as Address<"8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.selfProgram),
    ],
    data: getInitializeRedemptionInstructionDataEncoder().encode(
      args as InitializeRedemptionInstructionDataArgs,
    ),
    programAddress,
  } as InitializeRedemptionInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >);
}

export type InitializeRedemptionInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountSelfProgram extends string = string,
> = {
  /** Admin / deployer (payer for PDA creation) */
  admin: TransactionSigner<TAccountAdmin>;
  /** RedemptionConfig PDA (to be created) */
  config: Address<TAccountConfig>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Redemption program (self) for CPI event emission */
  selfProgram?: Address<TAccountSelfProgram>;
  permissionManager: InitializeRedemptionInstructionDataArgs["permissionManager"];
};

export function getInitializeRedemptionInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountSelfProgram extends string,
  TProgramAddress extends Address = typeof REDEMPTION_PROGRAM_ADDRESS,
>(
  input: InitializeRedemptionInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeRedemptionInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountSelfProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? REDEMPTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    selfProgram: { value: input.selfProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.selfProgram.value) {
    accounts.selfProgram.value =
      "8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX" as Address<"8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.selfProgram),
    ],
    data: getInitializeRedemptionInstructionDataEncoder().encode(
      args as InitializeRedemptionInstructionDataArgs,
    ),
    programAddress,
  } as InitializeRedemptionInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >);
}

export type ParsedInitializeRedemptionInstruction<
  TProgram extends string = typeof REDEMPTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin / deployer (payer for PDA creation) */
    admin: TAccountMetas[0];
    /** RedemptionConfig PDA (to be created) */
    config: TAccountMetas[1];
    /** System program */
    systemProgram: TAccountMetas[2];
    /** Event authority PDA for CPI event emission */
    eventAuthority: TAccountMetas[3];
    /** Redemption program (self) for CPI event emission */
    selfProgram: TAccountMetas[4];
  };
  data: InitializeRedemptionInstructionData;
};

export function parseInitializeRedemptionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeRedemptionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      selfProgram: getNextAccount(),
    },
    data: getInitializeRedemptionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
