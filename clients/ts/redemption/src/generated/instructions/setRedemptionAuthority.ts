/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findRedemptionConfigPda } from "../pdas";
import { REDEMPTION_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_REDEMPTION_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  236, 144, 84, 64, 171, 117, 177, 227,
]);

export function getSetRedemptionAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_REDEMPTION_AUTHORITY_DISCRIMINATOR,
  );
}

export type SetRedemptionAuthorityInstruction<
  TProgram extends string = typeof REDEMPTION_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRedemptionConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountRedemptionConfig extends string
        ? WritableAccount<TAccountRedemptionConfig>
        : TAccountRedemptionConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetRedemptionAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type SetRedemptionAuthorityInstructionDataArgs = {
  newAuthority: Address;
};

export function getSetRedemptionAuthorityInstructionDataEncoder(): FixedSizeEncoder<SetRedemptionAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_REDEMPTION_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getSetRedemptionAuthorityInstructionDataDecoder(): FixedSizeDecoder<SetRedemptionAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getSetRedemptionAuthorityInstructionDataCodec(): FixedSizeCodec<
  SetRedemptionAuthorityInstructionDataArgs,
  SetRedemptionAuthorityInstructionData
> {
  return combineCodec(
    getSetRedemptionAuthorityInstructionDataEncoder(),
    getSetRedemptionAuthorityInstructionDataDecoder(),
  );
}

export type SetRedemptionAuthorityAsyncInput<
  TAccountAdmin extends string = string,
  TAccountRedemptionConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  redemptionConfig?: Address<TAccountRedemptionConfig>;
  newAuthority: SetRedemptionAuthorityInstructionDataArgs["newAuthority"];
};

export async function getSetRedemptionAuthorityInstructionAsync<
  TAccountAdmin extends string,
  TAccountRedemptionConfig extends string,
  TProgramAddress extends Address = typeof REDEMPTION_PROGRAM_ADDRESS,
>(
  input: SetRedemptionAuthorityAsyncInput<
    TAccountAdmin,
    TAccountRedemptionConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetRedemptionAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRedemptionConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REDEMPTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    redemptionConfig: {
      value: input.redemptionConfig ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.redemptionConfig.value) {
    accounts.redemptionConfig.value = await findRedemptionConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.redemptionConfig),
    ],
    data: getSetRedemptionAuthorityInstructionDataEncoder().encode(
      args as SetRedemptionAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetRedemptionAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRedemptionConfig
  >);
}

export type SetRedemptionAuthorityInput<
  TAccountAdmin extends string = string,
  TAccountRedemptionConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  redemptionConfig: Address<TAccountRedemptionConfig>;
  newAuthority: SetRedemptionAuthorityInstructionDataArgs["newAuthority"];
};

export function getSetRedemptionAuthorityInstruction<
  TAccountAdmin extends string,
  TAccountRedemptionConfig extends string,
  TProgramAddress extends Address = typeof REDEMPTION_PROGRAM_ADDRESS,
>(
  input: SetRedemptionAuthorityInput<TAccountAdmin, TAccountRedemptionConfig>,
  config?: { programAddress?: TProgramAddress },
): SetRedemptionAuthorityInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountRedemptionConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? REDEMPTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    redemptionConfig: {
      value: input.redemptionConfig ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.redemptionConfig),
    ],
    data: getSetRedemptionAuthorityInstructionDataEncoder().encode(
      args as SetRedemptionAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetRedemptionAuthorityInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRedemptionConfig
  >);
}

export type ParsedSetRedemptionAuthorityInstruction<
  TProgram extends string = typeof REDEMPTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    redemptionConfig: TAccountMetas[1];
  };
  data: SetRedemptionAuthorityInstructionData;
};

export function parseSetRedemptionAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetRedemptionAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), redemptionConfig: getNextAccount() },
    data: getSetRedemptionAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
