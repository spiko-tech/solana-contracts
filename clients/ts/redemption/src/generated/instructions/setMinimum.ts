/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { REDEMPTION_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_MINIMUM_DISCRIMINATOR = 3;

export function getSetMinimumDiscriminatorBytes() {
  return getU8Encoder().encode(SET_MINIMUM_DISCRIMINATOR);
}

export type SetMinimumInstruction<
  TProgram extends string = typeof REDEMPTION_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPermConfig extends string | AccountMeta<string> = string,
  TAccountTokenMinimum extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountSelfProgram extends string | AccountMeta<string> =
    "8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPermConfig extends string
        ? ReadonlyAccount<TAccountPermConfig>
        : TAccountPermConfig,
      TAccountTokenMinimum extends string
        ? WritableAccount<TAccountTokenMinimum>
        : TAccountTokenMinimum,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountSelfProgram extends string
        ? ReadonlyAccount<TAccountSelfProgram>
        : TAccountSelfProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetMinimumInstructionData = {
  discriminator: number;
  tokenMint: Address;
  minimum: bigint;
};

export type SetMinimumInstructionDataArgs = {
  tokenMint: Address;
  minimum: number | bigint;
};

export function getSetMinimumInstructionDataEncoder(): FixedSizeEncoder<SetMinimumInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["tokenMint", getAddressEncoder()],
      ["minimum", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MINIMUM_DISCRIMINATOR }),
  );
}

export function getSetMinimumInstructionDataDecoder(): FixedSizeDecoder<SetMinimumInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["tokenMint", getAddressDecoder()],
    ["minimum", getU64Decoder()],
  ]);
}

export function getSetMinimumInstructionDataCodec(): FixedSizeCodec<
  SetMinimumInstructionDataArgs,
  SetMinimumInstructionData
> {
  return combineCodec(
    getSetMinimumInstructionDataEncoder(),
    getSetMinimumInstructionDataDecoder(),
  );
}

export type SetMinimumInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountPermConfig extends string = string,
  TAccountTokenMinimum extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountSelfProgram extends string = string,
> = {
  /** Admin caller (payer if creating TokenMinimum PDA) */
  caller: TransactionSigner<TAccountCaller>;
  /** RedemptionConfig PDA (to read permission_manager) */
  config: Address<TAccountConfig>;
  /** PermissionConfig PDA (from permission_manager, for admin check) */
  permConfig: Address<TAccountPermConfig>;
  /** TokenMinimum PDA (created if needed) */
  tokenMinimum: Address<TAccountTokenMinimum>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Redemption program (self) */
  selfProgram?: Address<TAccountSelfProgram>;
  tokenMint: SetMinimumInstructionDataArgs["tokenMint"];
  minimum: SetMinimumInstructionDataArgs["minimum"];
};

export function getSetMinimumInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountPermConfig extends string,
  TAccountTokenMinimum extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountSelfProgram extends string,
  TProgramAddress extends Address = typeof REDEMPTION_PROGRAM_ADDRESS,
>(
  input: SetMinimumInput<
    TAccountCaller,
    TAccountConfig,
    TAccountPermConfig,
    TAccountTokenMinimum,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetMinimumInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountPermConfig,
  TAccountTokenMinimum,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountSelfProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? REDEMPTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    permConfig: { value: input.permConfig ?? null, isWritable: false },
    tokenMinimum: { value: input.tokenMinimum ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    selfProgram: { value: input.selfProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.selfProgram.value) {
    accounts.selfProgram.value =
      "8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX" as Address<"8opABJP3fzXuCVUnbzDZqYpnfxmCmeiXUQ49txf6BFWX">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.permConfig),
      getAccountMeta(accounts.tokenMinimum),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.eventAuthority),
      getAccountMeta(accounts.selfProgram),
    ],
    data: getSetMinimumInstructionDataEncoder().encode(
      args as SetMinimumInstructionDataArgs,
    ),
    programAddress,
  } as SetMinimumInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountPermConfig,
    TAccountTokenMinimum,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountSelfProgram
  >);
}

export type ParsedSetMinimumInstruction<
  TProgram extends string = typeof REDEMPTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin caller (payer if creating TokenMinimum PDA) */
    caller: TAccountMetas[0];
    /** RedemptionConfig PDA (to read permission_manager) */
    config: TAccountMetas[1];
    /** PermissionConfig PDA (from permission_manager, for admin check) */
    permConfig: TAccountMetas[2];
    /** TokenMinimum PDA (created if needed) */
    tokenMinimum: TAccountMetas[3];
    /** System program */
    systemProgram: TAccountMetas[4];
    /** Event authority PDA */
    eventAuthority: TAccountMetas[5];
    /** Redemption program (self) */
    selfProgram: TAccountMetas[6];
  };
  data: SetMinimumInstructionData;
};

export function parseSetMinimumInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMinimumInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      permConfig: getNextAccount(),
      tokenMinimum: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      selfProgram: getNextAccount(),
    },
    data: getSetMinimumInstructionDataDecoder().decode(instruction.data),
  };
}
