/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import { findUserPermissionsPda, UserPermissionsSeeds } from "../pdas";

export const USER_PERMISSIONS_DISCRIMINATOR = 2;

export function getUserPermissionsDiscriminatorBytes() {
  return getU8Encoder().encode(USER_PERMISSIONS_DISCRIMINATOR);
}

export type UserPermissions = {
  discriminator: number;
  version: number;
  bump: number;
  roles: ReadonlyUint8Array;
};

export type UserPermissionsArgs = {
  discriminator?: number;
  version?: number;
  bump: number;
  roles: ReadonlyUint8Array;
};

/** Gets the encoder for {@link UserPermissionsArgs} account data. */
export function getUserPermissionsEncoder(): Encoder<UserPermissionsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["version", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["roles", getBytesEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: value.discriminator ?? USER_PERMISSIONS_DISCRIMINATOR,
      version: value.version ?? 1,
    }),
  );
}

/** Gets the decoder for {@link UserPermissions} account data. */
export function getUserPermissionsDecoder(): Decoder<UserPermissions> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["version", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["roles", getBytesDecoder()],
  ]);
}

/** Gets the codec for {@link UserPermissions} account data. */
export function getUserPermissionsCodec(): Codec<
  UserPermissionsArgs,
  UserPermissions
> {
  return combineCodec(getUserPermissionsEncoder(), getUserPermissionsDecoder());
}

export function decodeUserPermissions<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UserPermissions, TAddress>;
export function decodeUserPermissions<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UserPermissions, TAddress>;
export function decodeUserPermissions<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<UserPermissions, TAddress>
  | MaybeAccount<UserPermissions, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserPermissionsDecoder(),
  );
}

export async function fetchUserPermissions<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UserPermissions, TAddress>> {
  const maybeAccount = await fetchMaybeUserPermissions(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserPermissions<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UserPermissions, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserPermissions(maybeAccount);
}

export async function fetchAllUserPermissions(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UserPermissions>[]> {
  const maybeAccounts = await fetchAllMaybeUserPermissions(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserPermissions(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UserPermissions>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeUserPermissions(maybeAccount),
  );
}

export async function fetchUserPermissionsFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: UserPermissionsSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<UserPermissions>> {
  const maybeAccount = await fetchMaybeUserPermissionsFromSeeds(
    rpc,
    seeds,
    config,
  );
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserPermissionsFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: UserPermissionsSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<UserPermissions>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findUserPermissionsPda(seeds, { programAddress });
  return await fetchMaybeUserPermissions(rpc, address, fetchConfig);
}
