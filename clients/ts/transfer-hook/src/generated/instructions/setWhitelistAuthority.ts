/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findHookConfigPda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_WHITELIST_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  88, 124, 102, 66, 136, 116, 23, 209,
]);

export function getSetWhitelistAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_WHITELIST_AUTHORITY_DISCRIMINATOR,
  );
}

export type SetWhitelistAuthorityInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountHookConfig extends string
        ? WritableAccount<TAccountHookConfig>
        : TAccountHookConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetWhitelistAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type SetWhitelistAuthorityInstructionDataArgs = {
  newAuthority: Address;
};

export function getSetWhitelistAuthorityInstructionDataEncoder(): FixedSizeEncoder<SetWhitelistAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_WHITELIST_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getSetWhitelistAuthorityInstructionDataDecoder(): FixedSizeDecoder<SetWhitelistAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getSetWhitelistAuthorityInstructionDataCodec(): FixedSizeCodec<
  SetWhitelistAuthorityInstructionDataArgs,
  SetWhitelistAuthorityInstructionData
> {
  return combineCodec(
    getSetWhitelistAuthorityInstructionDataEncoder(),
    getSetWhitelistAuthorityInstructionDataDecoder(),
  );
}

export type SetWhitelistAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountHookConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  hookConfig?: Address<TAccountHookConfig>;
  newAuthority: SetWhitelistAuthorityInstructionDataArgs["newAuthority"];
};

export async function getSetWhitelistAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountHookConfig extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: SetWhitelistAuthorityAsyncInput<TAccountAuthority, TAccountHookConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetWhitelistAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.hookConfig),
    ],
    data: getSetWhitelistAuthorityInstructionDataEncoder().encode(
      args as SetWhitelistAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetWhitelistAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig
  >);
}

export type SetWhitelistAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountHookConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  hookConfig: Address<TAccountHookConfig>;
  newAuthority: SetWhitelistAuthorityInstructionDataArgs["newAuthority"];
};

export function getSetWhitelistAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountHookConfig extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: SetWhitelistAuthorityInput<TAccountAuthority, TAccountHookConfig>,
  config?: { programAddress?: TProgramAddress },
): SetWhitelistAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountHookConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.hookConfig),
    ],
    data: getSetWhitelistAuthorityInstructionDataEncoder().encode(
      args as SetWhitelistAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as SetWhitelistAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig
  >);
}

export type ParsedSetWhitelistAuthorityInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    hookConfig: TAccountMetas[1];
  };
  data: SetWhitelistAuthorityInstructionData;
};

export function parseSetWhitelistAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetWhitelistAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), hookConfig: getNextAccount() },
    data: getSetWhitelistAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
