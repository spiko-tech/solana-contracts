/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findHookConfigPda, findWhitelistStatePda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const UNWHITELIST_DISCRIMINATOR = new Uint8Array([
  142, 38, 153, 97, 115, 84, 197, 57,
]);

export function getUnwhitelistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UNWHITELIST_DISCRIMINATOR);
}

export type UnwhitelistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountWhitelistState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountWhitelistState extends string
        ? WritableAccount<TAccountWhitelistState>
        : TAccountWhitelistState,
      ...TRemainingAccounts,
    ]
  >;

export type UnwhitelistInstructionData = { discriminator: ReadonlyUint8Array };

export type UnwhitelistInstructionDataArgs = {};

export function getUnwhitelistInstructionDataEncoder(): FixedSizeEncoder<UnwhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UNWHITELIST_DISCRIMINATOR }),
  );
}

export function getUnwhitelistInstructionDataDecoder(): FixedSizeDecoder<UnwhitelistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnwhitelistInstructionDataCodec(): FixedSizeCodec<
  UnwhitelistInstructionDataArgs,
  UnwhitelistInstructionData
> {
  return combineCodec(
    getUnwhitelistInstructionDataEncoder(),
    getUnwhitelistInstructionDataDecoder(),
  );
}

export type UnwhitelistAsyncInput<
  TAccountAuthority extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  hookConfig?: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState?: Address<TAccountWhitelistState>;
};

export async function getUnwhitelistInstructionAsync<
  TAccountAuthority extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: UnwhitelistAsyncInput<
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UnwhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda();
  }
  if (!accounts.whitelistState.value) {
    accounts.whitelistState.value = await findWhitelistStatePda({
      wallet: expectAddress(accounts.wallet.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
    ],
    data: getUnwhitelistInstructionDataEncoder().encode({}),
    programAddress,
  } as UnwhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState
  >);
}

export type UnwhitelistInput<
  TAccountAuthority extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  hookConfig: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState: Address<TAccountWhitelistState>;
};

export function getUnwhitelistInstruction<
  TAccountAuthority extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: UnwhitelistInput<
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState
  >,
  config?: { programAddress?: TProgramAddress },
): UnwhitelistInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountHookConfig,
  TAccountWallet,
  TAccountWhitelistState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
    ],
    data: getUnwhitelistInstructionDataEncoder().encode({}),
    programAddress,
  } as UnwhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState
  >);
}

export type ParsedUnwhitelistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    hookConfig: TAccountMetas[1];
    wallet: TAccountMetas[2];
    whitelistState: TAccountMetas[3];
  };
  data: UnwhitelistInstructionData;
};

export function parseUnwhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnwhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      hookConfig: getNextAccount(),
      wallet: getNextAccount(),
      whitelistState: getNextAccount(),
    },
    data: getUnwhitelistInstructionDataDecoder().decode(instruction.data),
  };
}
