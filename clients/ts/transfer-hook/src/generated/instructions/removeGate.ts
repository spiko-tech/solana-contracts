/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findHookConfigPda, findWhitelistStatePda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REMOVE_GATE_DISCRIMINATOR = new Uint8Array([
  245, 146, 197, 200, 18, 28, 105, 56,
]);

export function getRemoveGateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REMOVE_GATE_DISCRIMINATOR);
}

export type RemoveGateInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountWhitelistState extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountWhitelistState extends string
        ? WritableAccount<TAccountWhitelistState>
        : TAccountWhitelistState,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveGateInstructionData = { discriminator: ReadonlyUint8Array };

export type RemoveGateInstructionDataArgs = {};

export function getRemoveGateInstructionDataEncoder(): FixedSizeEncoder<RemoveGateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REMOVE_GATE_DISCRIMINATOR }),
  );
}

export function getRemoveGateInstructionDataDecoder(): FixedSizeDecoder<RemoveGateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRemoveGateInstructionDataCodec(): FixedSizeCodec<
  RemoveGateInstructionDataArgs,
  RemoveGateInstructionData
> {
  return combineCodec(
    getRemoveGateInstructionDataEncoder(),
    getRemoveGateInstructionDataDecoder(),
  );
}

export type RemoveGateAsyncInput<
  TAccountAdmin extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
  TAccountPayer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  hookConfig?: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState?: Address<TAccountWhitelistState>;
  payer: TransactionSigner<TAccountPayer>;
};

export async function getRemoveGateInstructionAsync<
  TAccountAdmin extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: RemoveGateAsyncInput<
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveGateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda();
  }
  if (!accounts.whitelistState.value) {
    accounts.whitelistState.value = await findWhitelistStatePda({
      wallet: expectAddress(accounts.wallet.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
      getAccountMeta(accounts.payer),
    ],
    data: getRemoveGateInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveGateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer
  >);
}

export type RemoveGateInput<
  TAccountAdmin extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
  TAccountPayer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  hookConfig: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState: Address<TAccountWhitelistState>;
  payer: TransactionSigner<TAccountPayer>;
};

export function getRemoveGateInstruction<
  TAccountAdmin extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TAccountPayer extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: RemoveGateInput<
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveGateInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountHookConfig,
  TAccountWallet,
  TAccountWhitelistState,
  TAccountPayer
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
      getAccountMeta(accounts.payer),
    ],
    data: getRemoveGateInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveGateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer
  >);
}

export type ParsedRemoveGateInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    hookConfig: TAccountMetas[1];
    wallet: TAccountMetas[2];
    whitelistState: TAccountMetas[3];
    payer: TAccountMetas[4];
  };
  data: RemoveGateInstructionData;
};

export function parseRemoveGateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveGateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      hookConfig: getNextAccount(),
      wallet: getNextAccount(),
      whitelistState: getNextAccount(),
      payer: getNextAccount(),
    },
    data: getRemoveGateInstructionDataDecoder().decode(instruction.data),
  };
}
