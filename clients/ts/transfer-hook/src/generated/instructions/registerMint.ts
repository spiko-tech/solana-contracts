/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findExtraAccountMetasPda, findHookConfigPda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REGISTER_MINT_DISCRIMINATOR = new Uint8Array([
  242, 43, 74, 162, 217, 214, 191, 171,
]);

export function getRegisterMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_MINT_DISCRIMINATOR,
  );
}

export type RegisterMintInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountExtraAccountMetas extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountExtraAccountMetas extends string
        ? WritableAccount<TAccountExtraAccountMetas>
        : TAccountExtraAccountMetas,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterMintInstructionData = { discriminator: ReadonlyUint8Array };

export type RegisterMintInstructionDataArgs = {};

export function getRegisterMintInstructionDataEncoder(): FixedSizeEncoder<RegisterMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REGISTER_MINT_DISCRIMINATOR }),
  );
}

export function getRegisterMintInstructionDataDecoder(): FixedSizeDecoder<RegisterMintInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRegisterMintInstructionDataCodec(): FixedSizeCodec<
  RegisterMintInstructionDataArgs,
  RegisterMintInstructionData
> {
  return combineCodec(
    getRegisterMintInstructionDataEncoder(),
    getRegisterMintInstructionDataDecoder(),
  );
}

export type RegisterMintAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMint extends string = string,
  TAccountHookConfig extends string = string,
  TAccountExtraAccountMetas extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  mint: Address<TAccountMint>;
  hookConfig?: Address<TAccountHookConfig>;
  extraAccountMetas?: Address<TAccountExtraAccountMetas>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRegisterMintInstructionAsync<
  TAccountAdmin extends string,
  TAccountMint extends string,
  TAccountHookConfig extends string,
  TAccountExtraAccountMetas extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: RegisterMintAsyncInput<
    TAccountAdmin,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetas,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterMintInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetas,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    extraAccountMetas: {
      value: input.extraAccountMetas ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda();
  }
  if (!accounts.extraAccountMetas.value) {
    accounts.extraAccountMetas.value = await findExtraAccountMetasPda({
      mint: expectAddress(accounts.mint.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.extraAccountMetas),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterMintInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterMintInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetas,
    TAccountSystemProgram
  >);
}

export type RegisterMintInput<
  TAccountAdmin extends string = string,
  TAccountMint extends string = string,
  TAccountHookConfig extends string = string,
  TAccountExtraAccountMetas extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  mint: Address<TAccountMint>;
  hookConfig: Address<TAccountHookConfig>;
  extraAccountMetas: Address<TAccountExtraAccountMetas>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRegisterMintInstruction<
  TAccountAdmin extends string,
  TAccountMint extends string,
  TAccountHookConfig extends string,
  TAccountExtraAccountMetas extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: RegisterMintInput<
    TAccountAdmin,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetas,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterMintInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMint,
  TAccountHookConfig,
  TAccountExtraAccountMetas,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    extraAccountMetas: {
      value: input.extraAccountMetas ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.extraAccountMetas),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterMintInstructionDataEncoder().encode({}),
    programAddress,
  } as RegisterMintInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMint,
    TAccountHookConfig,
    TAccountExtraAccountMetas,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterMintInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    mint: TAccountMetas[1];
    hookConfig: TAccountMetas[2];
    extraAccountMetas: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterMintInstructionData;
};

export function parseRegisterMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      mint: getNextAccount(),
      hookConfig: getNextAccount(),
      extraAccountMetas: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterMintInstructionDataDecoder().decode(instruction.data),
  };
}
