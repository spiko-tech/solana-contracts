/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findHookConfigPda, findWhitelistStatePda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ADD_GATE_DISCRIMINATOR = new Uint8Array([
  89, 220, 103, 18, 25, 101, 194, 176,
]);

export function getAddGateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_GATE_DISCRIMINATOR);
}

export type AddGateInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountWhitelistState extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountWhitelistState extends string
        ? WritableAccount<TAccountWhitelistState>
        : TAccountWhitelistState,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddGateInstructionData = { discriminator: ReadonlyUint8Array };

export type AddGateInstructionDataArgs = {};

export function getAddGateInstructionDataEncoder(): FixedSizeEncoder<AddGateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADD_GATE_DISCRIMINATOR }),
  );
}

export function getAddGateInstructionDataDecoder(): FixedSizeDecoder<AddGateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAddGateInstructionDataCodec(): FixedSizeCodec<
  AddGateInstructionDataArgs,
  AddGateInstructionData
> {
  return combineCodec(
    getAddGateInstructionDataEncoder(),
    getAddGateInstructionDataDecoder(),
  );
}

export type AddGateAsyncInput<
  TAccountAdmin extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  hookConfig?: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState?: Address<TAccountWhitelistState>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAddGateInstructionAsync<
  TAccountAdmin extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: AddGateAsyncInput<
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddGateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda();
  }
  if (!accounts.whitelistState.value) {
    accounts.whitelistState.value = await findWhitelistStatePda({
      wallet: expectAddress(accounts.wallet.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddGateInstructionDataEncoder().encode({}),
    programAddress,
  } as AddGateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type AddGateInput<
  TAccountAdmin extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  hookConfig: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState: Address<TAccountWhitelistState>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAddGateInstruction<
  TAccountAdmin extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: AddGateInput<
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddGateInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountHookConfig,
  TAccountWallet,
  TAccountWhitelistState,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddGateInstructionDataEncoder().encode({}),
    programAddress,
  } as AddGateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedAddGateInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    hookConfig: TAccountMetas[1];
    wallet: TAccountMetas[2];
    whitelistState: TAccountMetas[3];
    payer: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: AddGateInstructionData;
};

export function parseAddGateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddGateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      hookConfig: getNextAccount(),
      wallet: getNextAccount(),
      whitelistState: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddGateInstructionDataDecoder().decode(instruction.data),
  };
}
