/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findHookConfigPda, findWhitelistStatePda } from "../pdas";
import { TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const WHITELIST_DISCRIMINATOR = new Uint8Array([
  0, 143, 193, 93, 69, 29, 183, 140,
]);

export function getWhitelistDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(WHITELIST_DISCRIMINATOR);
}

export type WhitelistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountWhitelistState extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountHookConfig extends string
        ? ReadonlyAccount<TAccountHookConfig>
        : TAccountHookConfig,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountWhitelistState extends string
        ? WritableAccount<TAccountWhitelistState>
        : TAccountWhitelistState,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WhitelistInstructionData = {
  discriminator: ReadonlyUint8Array;
  ext: boolean;
};

export type WhitelistInstructionDataArgs = { ext: boolean };

export function getWhitelistInstructionDataEncoder(): FixedSizeEncoder<WhitelistInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["ext", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: WHITELIST_DISCRIMINATOR }),
  );
}

export function getWhitelistInstructionDataDecoder(): FixedSizeDecoder<WhitelistInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["ext", getBooleanDecoder()],
  ]);
}

export function getWhitelistInstructionDataCodec(): FixedSizeCodec<
  WhitelistInstructionDataArgs,
  WhitelistInstructionData
> {
  return combineCodec(
    getWhitelistInstructionDataEncoder(),
    getWhitelistInstructionDataDecoder(),
  );
}

export type WhitelistAsyncInput<
  TAccountAuthority extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  hookConfig?: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState?: Address<TAccountWhitelistState>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  ext: WhitelistInstructionDataArgs["ext"];
};

export async function getWhitelistInstructionAsync<
  TAccountAuthority extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: WhitelistAsyncInput<
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda();
  }
  if (!accounts.whitelistState.value) {
    accounts.whitelistState.value = await findWhitelistStatePda({
      wallet: expectAddress(accounts.wallet.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWhitelistInstructionDataEncoder().encode(
      args as WhitelistInstructionDataArgs,
    ),
    programAddress,
  } as WhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type WhitelistInput<
  TAccountAuthority extends string = string,
  TAccountHookConfig extends string = string,
  TAccountWallet extends string = string,
  TAccountWhitelistState extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  hookConfig: Address<TAccountHookConfig>;
  wallet: Address<TAccountWallet>;
  whitelistState: Address<TAccountWhitelistState>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  ext: WhitelistInstructionDataArgs["ext"];
};

export function getWhitelistInstruction<
  TAccountAuthority extends string,
  TAccountHookConfig extends string,
  TAccountWallet extends string,
  TAccountWhitelistState extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: WhitelistInput<
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): WhitelistInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountHookConfig,
  TAccountWallet,
  TAccountWhitelistState,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: false },
    wallet: { value: input.wallet ?? null, isWritable: false },
    whitelistState: { value: input.whitelistState ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.hookConfig),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.whitelistState),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWhitelistInstructionDataEncoder().encode(
      args as WhitelistInstructionDataArgs,
    ),
    programAddress,
  } as WhitelistInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountHookConfig,
    TAccountWallet,
    TAccountWhitelistState,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedWhitelistInstruction<
  TProgram extends string = typeof TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    hookConfig: TAccountMetas[1];
    wallet: TAccountMetas[2];
    whitelistState: TAccountMetas[3];
    payer: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: WhitelistInstructionData;
};

export function parseWhitelistInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWhitelistInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      hookConfig: getNextAccount(),
      wallet: getNextAccount(),
      whitelistState: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWhitelistInstructionDataDecoder().decode(instruction.data),
  };
}
