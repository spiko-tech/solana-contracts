/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findHookConfigPda } from "../pdas";
import { SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const UNPAUSE_HOOK_DISCRIMINATOR = new Uint8Array([
  7, 217, 254, 136, 32, 179, 7, 226,
]);

export function getUnpauseHookDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNPAUSE_HOOK_DISCRIMINATOR,
  );
}

export type UnpauseHookInstruction<
  TProgram extends string = typeof SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountMintAuthority extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountHookConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountMintAuthority extends string
        ? ReadonlySignerAccount<TAccountMintAuthority> &
            AccountSignerMeta<TAccountMintAuthority>
        : TAccountMintAuthority,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountHookConfig extends string
        ? WritableAccount<TAccountHookConfig>
        : TAccountHookConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UnpauseHookInstructionData = { discriminator: ReadonlyUint8Array };

export type UnpauseHookInstructionDataArgs = {};

export function getUnpauseHookInstructionDataEncoder(): FixedSizeEncoder<UnpauseHookInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UNPAUSE_HOOK_DISCRIMINATOR }),
  );
}

export function getUnpauseHookInstructionDataDecoder(): FixedSizeDecoder<UnpauseHookInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnpauseHookInstructionDataCodec(): FixedSizeCodec<
  UnpauseHookInstructionDataArgs,
  UnpauseHookInstructionData
> {
  return combineCodec(
    getUnpauseHookInstructionDataEncoder(),
    getUnpauseHookInstructionDataDecoder(),
  );
}

export type UnpauseHookAsyncInput<
  TAccountCaller extends string = string,
  TAccountMintAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountHookConfig extends string = string,
> = {
  /** The original caller (admin/pauser). Auth is checked by spiko-token before CPI. */
  caller: TransactionSigner<TAccountCaller>;
  /** spiko-token MintAuthority PDA — must be a signer, enforcing CPI-only access. */
  mintAuthority: TransactionSigner<TAccountMintAuthority>;
  mint: Address<TAccountMint>;
  hookConfig?: Address<TAccountHookConfig>;
};

export async function getUnpauseHookInstructionAsync<
  TAccountCaller extends string,
  TAccountMintAuthority extends string,
  TAccountMint extends string,
  TAccountHookConfig extends string,
  TProgramAddress extends Address = typeof SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: UnpauseHookAsyncInput<
    TAccountCaller,
    TAccountMintAuthority,
    TAccountMint,
    TAccountHookConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UnpauseHookInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountMintAuthority,
    TAccountMint,
    TAccountHookConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    mintAuthority: { value: input.mintAuthority ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.hookConfig.value) {
    accounts.hookConfig.value = await findHookConfigPda({
      mint: expectAddress(accounts.mint.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.mintAuthority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.hookConfig),
    ],
    data: getUnpauseHookInstructionDataEncoder().encode({}),
    programAddress,
  } as UnpauseHookInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountMintAuthority,
    TAccountMint,
    TAccountHookConfig
  >);
}

export type UnpauseHookInput<
  TAccountCaller extends string = string,
  TAccountMintAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountHookConfig extends string = string,
> = {
  /** The original caller (admin/pauser). Auth is checked by spiko-token before CPI. */
  caller: TransactionSigner<TAccountCaller>;
  /** spiko-token MintAuthority PDA — must be a signer, enforcing CPI-only access. */
  mintAuthority: TransactionSigner<TAccountMintAuthority>;
  mint: Address<TAccountMint>;
  hookConfig: Address<TAccountHookConfig>;
};

export function getUnpauseHookInstruction<
  TAccountCaller extends string,
  TAccountMintAuthority extends string,
  TAccountMint extends string,
  TAccountHookConfig extends string,
  TProgramAddress extends Address = typeof SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS,
>(
  input: UnpauseHookInput<
    TAccountCaller,
    TAccountMintAuthority,
    TAccountMint,
    TAccountHookConfig
  >,
  config?: { programAddress?: TProgramAddress },
): UnpauseHookInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountMintAuthority,
  TAccountMint,
  TAccountHookConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    mintAuthority: { value: input.mintAuthority ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    hookConfig: { value: input.hookConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.mintAuthority),
      getAccountMeta(accounts.mint),
      getAccountMeta(accounts.hookConfig),
    ],
    data: getUnpauseHookInstructionDataEncoder().encode({}),
    programAddress,
  } as UnpauseHookInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountMintAuthority,
    TAccountMint,
    TAccountHookConfig
  >);
}

export type ParsedUnpauseHookInstruction<
  TProgram extends string = typeof SPIKO_TRANSFER_HOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The original caller (admin/pauser). Auth is checked by spiko-token before CPI. */
    caller: TAccountMetas[0];
    /** spiko-token MintAuthority PDA — must be a signer, enforcing CPI-only access. */
    mintAuthority: TAccountMetas[1];
    mint: TAccountMetas[2];
    hookConfig: TAccountMetas[3];
  };
  data: UnpauseHookInstructionData;
};

export function parseUnpauseHookInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnpauseHookInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      mintAuthority: getNextAccount(),
      mint: getNextAccount(),
      hookConfig: getNextAccount(),
    },
    data: getUnpauseHookInstructionDataDecoder().decode(instruction.data),
  };
}
