[package]
name = "spiko-common"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pinocchio = { workspace = true }
pinocchio-system = { workspace = true }

[target.'cfg(not(target_os = "solana"))'.dependencies]
sha2 = "0.10"
